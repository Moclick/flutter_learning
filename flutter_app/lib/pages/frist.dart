import 'package:flutter/material.dart';

class FristPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
          child: Text("this is frist page")
        );
  }
}